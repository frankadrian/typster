'use strict';

describe('Typster App: MainController', function () {

    beforeEach(function () {
        //        browser().navigateTo('/#/');
    });

    /*    it('should redirect to a new unique hash page', function () {
     browser().navigateTo('/#/');
     expect(browser().window().hash()).toContain('/snippets/');
     });

     it('should be able to create a new item', function () {
     element('.js-create-modal').click();
     input('title').enter('created');
     input('text').enter('created');
     element('.js-create').click();

     });


     it('should be able to edit a new item', function () {
     element('.edit-btn').click();
     input('title').enter('edited');
     input('text').enter('edited');
     element('.js-update').click();

     });

     it('should delete a item', function () {
     element('.delete-btn').click();
     });*/
});
