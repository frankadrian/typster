#!/usr/bin/env bash

#current directory
DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"

echo "--- Let's get to work. Installing compass. ---"

echo "--- running 'gem install compass' ---"
gem install compass


echo "--- All done, enjoy! :) ---"